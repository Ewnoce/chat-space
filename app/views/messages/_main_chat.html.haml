.chat-main 
  .chat-main__group-info
    .chat-main__group-info__left-box

      %h3.left-box__current-group
        = @group.name
      .left-box__menber-list
        Member :
        .left-box__menber-list__member
          - @group.group_users.each do |group_user|
            = group_user.user.name
    .chat-main__group-info__edit-btn
      = link_to edit_group_path(@group), class: "edit" do
        Edit
  .chat-main__message-list
    = render @messages
  .chat-main__message-form
    = form_for [@group, @message] do |f|
      .message
        = f.text_field :content, class: 'message__input', placeholder: 'type a message'
        .message__btn
          = f.label :image, class: 'image' do
            = icon('far', 'image', class: 'icon')
            = f.file_field :image, class: 'hidden'
      = f.submit 'Send', class: 'submit'
        